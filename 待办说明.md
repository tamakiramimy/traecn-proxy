# 待办说明

## TODO-1：按账号类型自动切换服务面（模型列表 / chat 通道 / 请求参数）

状态：**已完成**。服务面描述对象见 [TraeServiceFace.cs](TraeServiceFace.cs)，账号类型持久化在 `accounts.json` 的 `kind` 字段。

### 原问题（重构前）

「用哪套协议」不是根据账号类型判断的，而是根据 **是否配置了 `ChatApiHost`** 这一个布尔值推断出来的：

```csharp
// TraeClient.cs
_usesExternalChatApiHost = !string.Equals(_apiHost, _chatApiHost, StringComparison.OrdinalIgnoreCase);
```

这个标志同时决定了四件互相独立的事，任何一处判断错都会导致静默降级或 `3003` / `4001` 类错误：

| 决策点 | 企业控制面 | 独立 SOLO 服务面 |
| --- | --- | --- |
| chat 通道 `function` | `chat_v3` | `solo_work_lite` |
| 模型目录端点 | `/api/ide/v1/batch_get_detail_param` | `/api/ide/v1/get_detail_param` |
| 目录解析器 | `TraeModelCatalogParser.Parse` | `ParseChatConfigs` |
| 客户端画像头 | IDE `3.3.87` / 本机设备 | SOLO `0.1.43` / `Windows 11 Pro` / `83DG` |

### 实现落点

- [TraeServiceFace.cs](TraeServiceFace.cs)：`TraeAccountKind`（auto / enterprise / solo）、`TraeClientProfile` 与两个服务面的不可变定义，上表四个决策点收敛于此
- [TraeClient.cs](TraeClient.cs)：构造时按账号类型选出服务面，chat 请求体、目录端点与请求头全部取自服务面
- [MultiAccount.cs](MultiAccount.cs)：`TraeAccount.Kind` 持久化到 `accounts.json`，运行时按账号实例化对应服务面
- [ProxySettings.cs](ProxySettings.cs)：`Upstream:DefaultAccountKind` 与 `Upstream:Enterprise` / `Upstream:Solo` 画像覆盖，版本号与设备信息不再需要改代码发版
- 默认模型由服务面提供（企业面 `Doubao-Seed-Evolving__dev`，SOLO 面 `Doubao-Seed-Evolving`），请求未带 `model` 时按当前账号解析

### 未覆盖项

账号类型目前只支持「显式指定」与「按 `ChatApiHost` 推断」两级。下面两个来源尚未实现，需要真实账号取证后再动：

- 从登录 / `GetUserInfo` 返回的租户信息推断类型
- 按目录端点探测一次并缓存的兜底

### 目标设计

1. **显式账号类型**。在 `TraeAccount` 上增加账号类型（如 `Enterprise` / `Solo` / `Consumer`），来源优先级：
   - 账号配置显式指定
   - 登录/`GetUserInfo` 返回的租户信息推断
   - 兜底：按目录端点探测一次并缓存
2. **抽出服务面描述对象**，把上表四个决策点收敛成一个不可变配置（通道名、目录端点、解析器、客户端画像、默认模型、模型 ID 语义），由账号类型选出，替换掉 `_usesExternalChatApiHost` 分支。
3. **客户端画像可配置**。版本号、设备品牌、OS 版本移到 `appsettings.json`，代码只提供默认值，上游变更时改配置即可，不必重新发版。
4. **默认模型随服务面解析**，不再用单一常量。

### 完成标准

- 不再有任何以 `_usesExternalChatApiHost` 作为协议分支依据的代码 —— 已移除，改由 `TraeServiceFace.Resolve` 选出不可变服务面
- 同一账号池内混合不同类型账号时，各自使用正确的通道与目录，互不影响 —— 服务面随 `TraeAccount.Kind` 按账号实例化
- 单测覆盖：每种账号类型的 chat 请求体（`function` / `config_name` / `model`）、目录端点选择、画像头 —— 见 [TraeServiceFaceTests.cs](tests/TrancnProxy.Tests/TraeServiceFaceTests.cs)
- 已验证模型回归不劣化（见下）

### 现有已验证结论（重构后必须保持）

企业控制面 `chat_v3` + `config_name`，上游 metadata 精确回显实际模型：

- `glm-5.3__dev` / `glm-5.3__max` → `glm-5.3`
- `qwen3.8-max__dev` → `qwen3.8-max`
- `kimi-k2.7-code__dev` → `kimi-k2.7-code`
- `DeepSeek-V4-Pro-Official__dev` → `ali-deepseek-v4-pro-0813`（服务商内部名）
- `deepseek_v4_flash_official__dev` → `deepseek-v4-flash-0731-ark`
- `minimax-m3__dev` / `minimax-m2.7__dev` / `glm-5v-turbo__dev` / `glm-5.2__dev` → 精确匹配

已知错误码语义：

- `3003`：chat 通道用错（如对 SOLO 面发 `inline_chat`）
- `4001`：该服务面不存在此模型
- `1005`：模型存在但账号权益不足

模型选择校验采用「实际模型必须包含所选模型」的归一化包含判定，用于接受服务商内部命名，同时拦截静默降级（豆包回退、`glm-5.3` → `glm-5`）。重构时不可放宽。

### 不在本项范围

- `kimi-k3` 仅存在于消费版 SOLO 目录且返回 `1005`，属权益问题，非协议问题
