# keys: file_path,content
# source: glm-5.3, 2026-09-02 —— content 正常闭合后多出一个 ")"，jsonrepair 报 Object key expected
{"file_path":"/tmp/whack.html","content":"<!DOCTYPE html>\n<html lang=\"zh-CN\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0, user-scalable=no\">\n<title>打地鼠 · Whack-a-Mole</title>\n<style>\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  html,body { height:100%; }\n  body {\n    font-family: \"PingFang SC\",\"Microsoft YaHei\",system-ui,sans-serif;\n    background: radial-gradient(circle at 50% 0%, #7ec850 0%, #4a9a3a 45%, #2e6b24 100%);\n    display:flex; flex-direction:column; align-items:center; justify-content:center;\n    min-height:100vh; padding:16px; overflow:hidden; user-select:none;\n  }\n  h1 {\n    color:#fff; text-shadow:0 3px 0 rgba(0,0,0,.25); letter-spacing:2px;\n    font-size:clamp(22px,5vw,36px); margin-bottom:12px;\n  }\n  .hud {\n    display:flex; gap:10px; flex-wrap:wrap; justify-content:center; margin-bottom:14px;\n  }\n  .card {\n    background:rgba(0,0,0,.28); border:2px solid rgba(255,255,255,.25);\n    border-radius:14px; padding:8px 18px; text-align:center; min-width:96px;\n    color:#fff; box-shadow:0 4px 12px rgba(0,0,0,.2);\n  }\n  .card .label { font-size:12px; opacity:.8; }\n  .card .value { font-size:clamp(20px,4.5vw,30px); font-weight:700; font-variant-numeric:tabular-nums; }\n  #btn {\n    background:linear-gradient(#ffb340,#e9871a); color:#fff; border:none; cursor:pointer;\n    font-size:16px; font-weight:700; padding:10px 34px; border-radius:999px;\n    box-shadow:0 5px 0 #a85d0d, 0 8px 16px rgba(0,0,0,.3); margin-bottom:14px;\n    letter-spacing:1px; transition:transform .08s, box-shadow .08s;\n  }\n  #btn:active { transform:translateY(4px); box-shadow:0 1px 0 #a85d0d; }\n  .board {\n    display:grid; grid-template-columns:repeat(3,1fr); gap:clamp(8px,2vw,18px);\n    width:min(92vw, 480px);\n  }\n  .hole {\n    position:relative; aspect-ratio:1/1; cursor:url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='48' height='48'><text y='36' font-size='36'>🔨</text></svg>\") 24 24, pointer;\n  }\n  .dirt {\n    position:absolute; inset:auto 0 0 0; height:26%; border-radius:50%;\n    background:radial-gradient(ellipse at 50% 40%, #5b3a1e 0%, #3b2412 70%, #2a1809 100%);\n    box-shadow:inset 0 6px 10px rgba(0,0,0,.7), 0 4px 8px rgba(0,0,0,.3);\n    z-index:2;\n  }\n  .mole {\n    position:absolute; left:50%; bottom:6%; width:68%; transform:translate(-50%,100%) scale(.6);\n    transition:transform .13s cubic-bezier(.2,.9,.3,1.3); z-index:1; pointer-events:none;\n  }\n  .hole.up .mole { transform:translate(-50%,18%) scale(1); }\n  .mole svg { display:block; width:100%; filter:drop-shadow(0 4px 4px rgba(0,0,0,.35)); }\n  .hole.whacked .mole { animation:whack .28s ease-out; }\n  @keyframes whack {\n    0% { filter:brightness(1); }\n    30% { filter:brightness(2.2) saturate(.4); transform:translate(-50%,30%) scale(.92) rotate(-8deg); }\n    100% { filter:brightness(1); }\n  }\n  .hole.bonked { animation:shake .25s; }\n  @keyframes shake {\n    0%,100% { transform:translateX(0); }\n    25% { transform:translateX(-5px); }\n    75% { transform:translateX(5px); }\n  }\n  .pop {\n    position:absolute; left:50%; top:20%; transform:translateX(-50%);\n    color:#ffe93b; font-weight:900; font-size:clamp(20px,5vw,32px);\n    text-shadow:0 2px 0 #b3541e, 0 0 12px rgba(255,180,0,.6);\n    pointer-events:none; z-index:5; animation:popup .7s ease-out forwards;\n  }\n  @keyframes popup {\n    0% { opacity:1; transform:translate(-50%,0) scale(.5); }\n    100% { opacity:0; transform:translate(-50%,-70px) scale(1.25); }\n  }\n  .overlay {\n    position:fixed; inset:0; background:rgba(10,30,10,.82); backdrop-filter:blur(4px);\n    display:flex; flex-direction:column; align-items:center; justify-content:center;\n    gap:14px; z-index:50; color:#fff; text-align:center; padding:24px;\n  }\n  .overlay.hidden { display:none; }\n  .overlay h2 { font-size:clamp(26px,6vw,44px); text-shadow:0 3px 0 rgba(0,0,0,.4); }\n  .overlay p { opacity:.9; line-height:1.7; max-width:340px; }\n  .overlay .big { font-size:clamp(40px,10vw,72px); font-weight:900; color:#ffe93b; text-shadow:0 4px 0 #b3541e; }\n  .combo-flash {\n    position:fixed; left:50%; top:38%; transform:translate(-50%,-50%);\n    color:#ff5db1; font-weight:900; font-size:clamp(28px,7vw,52px);\n    text-shadow:0 3px 0 #7a1350; pointer-events:none; z-index:40;\n    animation:combo .8s ease-out forwards;\n  }\n  @keyframes combo {\n    0% { opacity:0; transform:translate(-50%,-50%) scale(.4) rotate(-6deg); }\n    25% { opacity:1; transform:translate(-50%,-50%) scale(1.15) rotate(3deg); }\n    100% { opacity:0; transform:translate(-50%,-50%) scale(1) translateY(-40px); }\n  }\n  .footer-tip { color:rgba(255,255,255,.65); font-size:12px; margin-top:14px; }\n</style>\n</head>\n<body>\n<h1>🔨 打地鼠</h1>\n\n<div class=\"hud\">\n  <div class=\"card\"><div class=\"label\">得分</div><div class=\"value\" id=\"score\">0</div></div>\n  <div class=\"card\"><div class=\"label\">时间</div><div class=\"value\" id=\"time\">30</div></div>\n  <div class=\"card\"><div class=\"label\">连击</div><div class=\"value\" id=\"combo\">x0</div></div>\n  <div class=\"card\"><div class=\"label\">最高分</div><div class=\"value\" id=\"best\">0</div></div>\n</div>\n\n<button id=\"btn\">开始游戏</button>\n\n<div class=\"board\" id=\"board\"></div>\n<div class=\"footer-tip\">点击 / 触摸冒头的地鼠 · 连击有惊喜 · 普通地鼠 +10，金地鼠 +30</div>\n\n<div class=\"overlay\" id=\"overlay\">\n  <h2 id=\"ovTitle\">准备好了吗？</h2>\n  <p id=\"ovDesc\">30 秒内尽可能多地敲中地鼠！<br>越到后面地鼠冒得越快，抓住金地鼠拿高分！</p>\n  <div class=\"big\" id=\"ovScore\" style=\"display:none\">0</div>\n  <button id=\"ovBtn\" style=\"background:linear-gradient(#ffb340,#e9871a);color:#fff;border:none;cursor:pointer;font-size:18px;font-weight:700;padding:12px 44px;border-radius:999px;box-shadow:0 5px 0 #a85d0d;\">开始</button>\n</div>\n\n<script>\n(function(){\n  'use strict';\n\n  // ---------- 元素 ----------\n  const boardEl = document.getElementById('board');\n  const scoreEl = document.getElementById('score');\n  const timeEl  = document.getElementById('time');\n  const comboEl = document.getElementById('combo');\n  const bestEl  = document.getElementById('best');\n  const btn     = document.getElementById('btn');\n  const overlay = document.getElementById('overlay');\n  const ovTitle = document.getElementById('ovTitle');\n  const ovDesc  = document.getElementById('ovDesc');\n  const ovScore = document.getElementById('ovScore');\n  const ovBtn   = document.getElementById('ovBtn');\n\n  const HOLES = 9;\n  const GAME_TIME = 30;\n\n  // ---------- 状态 ----------\n  let running = false;\n  let score = 0;\n  let timeLeft = GAME_TIME;\n  let combo = 0;\n  let best = Number(localStorage.getItem('whack_best') || 0);\n  let moleTimers = [];      // 每个地鼠的隐藏定时器\n  let spawnTimer = null;\n  let tickTimer = null;\n  bestEl.textContent = best;\n\n  // ---------- 地鼠 SVG ----------\n  function moleSVG(golden){\n    const body = golden ? '#ffd24a' : '#a9744a';\n    const dark = golden ? '#d9a419' : '#855a37';\n    const belly = golden ? '#fff0b8' : '#d8b48e';\n    return `\n    <svg viewBox=\"0 0 100 110\" xmlns=\"http://www.w3.org/2000/svg\">\n      <ellipse cx=\"50\" cy=\"104\" rx=\"34\" ry=\"6\" fill=\"rgba(0,0,0,.25)\"/>\n      <path d=\"M50 8 C72 8 88 30 88 58 C88 86 72 104 50 104 C28 104 12 86 12 58 C12 30 28 8 50 8 Z\" fill=\"${body}\" stroke=\"${dark}\" stroke-width=\"3\"/>\n      <ellipse cx=\"50\" cy=\"74\" rx=\"24\" ry=\"22\" fill=\"${belly}\"/>\n      <ellipse cx=\"38\" cy=\"46\" rx=\"7\" ry=\"9\" fill=\"#fff\" stroke=\"${dark}\" stroke-width=\"1.5\"/>\n      <ellipse cx=\"62\" cy=\"46\" rx=\"7\" ry=\"9\" fill=\"#fff\" stroke=\"${dark}\" stroke-width=\"1.5\"/>\n      <circle cx=\"39\" cy=\"48\" r=\"3.4\" fill=\"#231a12\"/>\n      <circle cx=\"61\" cy=\"48\" r=\"3.4\" fill=\"#231a12\"/>\n      <circle cx=\"40\" cy=\"46.6\" r=\"1.1\" fill=\"#fff\"/>\n      <circle cx=\"62\" cy=\"46.6\" r=\"1.1\" fill=\"#fff\"/>\n      <ellipse cx=\"50\" cy=\"62\" rx=\"7\" ry=\"5.4\" fill=\"${dark}\"/>\n      <path d=\"M42 60 Q50 56 58 60\" stroke=\"#231a12\" stroke-width=\"2.4\" fill=\"none\" stroke-linecap=\"round\"/>\n      <path d=\"M44 68 Q50 73 56 68\" stroke=\"#7a2f2f\" stroke-width=\"3\" fill=\"none\" stroke-linecap=\"round\"/>\n      <path d=\"M30 20 Q34 6 46 10\" stroke=\"${dark}\" stroke-width=\"3\" fill=\"none\" stroke-linecap=\"round\"/>\n      <path d=\"M70 20 Q66 6 54 10\" stroke=\"${dark}\" stroke-width=\"3\" fill=\"none\" stroke-linecap=\"round\"/>\n      <path d=\"M28 66 Q20 60 22 52\" stroke=\"${dark}\" stroke-width=\"3\" fill=\"none\" stroke-linecap=\"round\"/>\n      <path d=\"M72 66 Q80 60 78 52\" stroke=\"${dark}\" stroke-width=\"3\" fill=\"none\" stroke-linecap=\"round\"/>\n      ${golden ? '<circle cx=\"26\" cy=\"24\" r=\"3\" fill=\"#fff8d0\"/><circle cx=\"76\" cy=\"30\" r=\"2.4\" fill=\"#fff8d0\"/><circle cx=\"50\" cy=\"20\" r=\"2\" fill=\"#fff8d0\"/>' : ''}\n    </svg>`;\n  }\n\n  // ---------- 生成洞 ----------\n  const holes = [];\n  for (let i = 0; i < HOLES; i++) {\n    const hole = document.createElement('div');\n    hole.className = 'hole';\n    hole.innerHTML = `<div class=\"mole\"></div><div class=\"dirt\"></div>`;\n    hole.addEventListener('pointerdown', e => whack(i, e));\n    boardEl.appendChild(hole);\n    holes.push({ el: hole, moleEl: hole.querySelector('.mole'), active: false, golden: false, timer: null });\n  }\n\n  // ---------- 音效（Web Audio） ----------\n  let audioCtx = null;\n  function ensureAudio(){\n    if (!audioCtx) { try { audioCtx = new (window.AudioContext || window.webkitAudioContext)(); } catch(e){} }\n    if (audioCtx && audioCtx.state === 'suspended') audioCtx.resume();\n  }\n  function beep(freq, dur, type, vol){\n    if (!audioCtx) return;\n    const o = audioCtx.createOscillator(), g = audioCtx.createGain();\n    o.type = type || 'square'; o.frequency.value = freq;\n    g.gain.setValueAtTime(vol || .15, audioCtx.currentTime);\n    g.gain.exponentialRampToValueAtTime(.001, audioCtx.currentTime + dur);\n    o.connect(g); g.connect(audioCtx.destination);\n    o.start(); o.stop(audioCtx.currentTime + dur);\n  }\n  const sHit   = () => { beep(220,.09,'square',.18); setTimeout(()=>beep(440,.08,'square',.14),40); };\n  const sGold  = () => { [660,880,1320].forEach((f,i)=>setTimeout(()=>beep(f,.12,'triangle',.16), i*70)); };\n  const sMiss  = () => beep(120,.15,'sawtooth',.1);\n  const sCombo = () => { [520,660,780].forEach((f,i)=>setTimeout(()=>beep(f,.1,'sine',.14), i*55)); };\n  const sEnd   = () => { [440,330,220].forEach((f,i)=>setTimeout(()=>beep(f,.22,'triangle',.16), i*180)); };\n\n  // ---------- 工具 ----------\n  function floatText(holeIdx, text, color){\n    const p = document.createElement('div');\n    p.className = 'pop';\n    if (color) { p.style.color = color; p.style.textShadow = '0 2px 0 #8a6d00'; }\n    p.textContent = text;\n    holes[holeIdx].el.appendChild(p);\n    setTimeout(()=>p.remove(), 750);\n  }\n\n  function comboFlash(){\n    const d = document.createElement('div');\n    d.className = 'combo-flash';\n    d.textContent = '🔥 ' + combo + ' 连击!';\n    document.body.appendChild(d);\n    setTimeout(()=>d.remove(), 850);\n  }\n\n  // ---------- 游戏逻辑 ----------\n  function showMole(){\n    const free = holes.filter(h => !h.active);\n    if (!free.length) return;\n    const h = free[Math.floor(Math.random() * free.length)];\n    h.golden = Math.random() < 0.12;\n    h.moleEl.innerHTML = moleSVG(h.golden);\n    h.active = true;\n    h.el.classList.remove('whacked');\n    // 触发重排以确保动画可重复\n    void h.el.offsetWidth;\n    h.el.classList.add('up');\n\n    // 停留时间随进度缩短\n    const progress = 1 - timeLeft / GAME_TIME;\n    const stay = (900 - progress * 450) + Math.random() * 350;\n    h.timer = setTimeout(() => hideMole(h), stay);\n  }\n\n  function hideMole(h){\n    clearTimeout(h.timer);\n    h.active = false;\n    h.el.classList.remove('up');\n  }\n\n  function whack(idx, e){\n    e.preventDefault();\n    ensureAudio();\n    const h = holes[idx];\n    if (!running) return;\n    if (!h.active || !h.el.classList.contains('up')) {\n      // 打空\n      combo = 0; comboEl.textContent = 'x0';\n      h.el.classList.remove('bonked'); void h.el.offsetWidth; h.el.classList.add('bonked');\n      floatText(idx, 'MISS', '#cfd8dc');\n      sMiss();\n      return;\n    }\n    const wasGolden = h.golden;\n    hideMole(h);\n    h.el.classList.remove('whacked'); void h.el.offsetWidth;\n    h.el.classList.add('whacked');\n\n    combo++;\n    comboEl.textContent = 'x' + combo;\n\n    let pts = wasGolden ? 30 : 10;\n    if (combo > 0 && combo % 5 === 0) { pts += 20; comboFlash(); sCombo(); }\n\n    score += pts;\n    scoreEl.textContent = score;\n    floatText(idx, '+' + pts, wasGolden ? '#ffd24a' : null);\n    wasGolden ? sGold() : sHit();\n  }\n\n  function spawnLoop(){\n    if (!running) return;\n    showMole();\n    // 有几率一次冒两只\n    if (Math.random() < .3) showMole();\n    const progress = 1 - timeLeft / GAME_TIME;\n    const delay = Math.max(280, 800 - progress * 480) + Math.random() * 200;\n    spawnTimer = setTimeout(spawnLoop, delay);\n  }\n\n  function tick(){\n    timeLeft--;\n    timeEl.textContent = Math.max(0, timeLeft);\n    if (timeLeft <= 5 && timeLeft > 0) beep(880, .07, 'sine', .1);\n    if (timeLeft <= 0) endGame();\n  }\n\n  function startGame(){\n    ensureAudio();\n    running = true;\n    score = 0; timeLeft = GAME_TIME; combo = 0;\n    scoreEl.textContent = '0';\n    timeEl.textContent = GAME_TIME;\n    comboEl.textContent = 'x0';\n    overlay.classList.add('hidden');\n    btn.textContent = '重新开始';\n    holes.forEach(hideMole);\n    tickTimer = setInterval(tick, 1000);\n    spawnLoop();\n    beep(660, .12, 'triangle', .14);\n  }\n\n  function endGame(){\n    running = false;\n    clearTimeout(spawnTimer);\n    clearInterval(tickTimer);\n    holes.forEach(hideMole);\n    sEnd();\n\n    const isRecord = score > best;\n    if (isRecord) { best = score; localStorage.setItem('whack_best', best); bestEl.textContent = best; }\n\n    ovTitle.textContent = isRecord ? '🏆 新纪录！' : '时间到！';\n    ovDesc.innerHTML = isRecord\n      ? '太厉害了，刷新最高分！<br>再来一局挑战更高？'\n      : `本局命中得分如下，再接再厉！<br>普通 +10 · 金鼠 +30 · 每 5 连击额外 +20`;\n    ovScore.style.display = 'block';\n    ovScore.textContent = score;\n    ovBtn.textContent = '再来一局';\n    overlay.classList.remove('hidden');\n  }\n\n  btn.addEventListener('click', () => { if (!running) startGame(); });\n  ovBtn.addEventListener('click', startGame);\n\n  // 键盘 1-9 快速敲击\n  document.addEventListener('keydown', e => {\n    const n = parseInt(e.key, 10);\n    if (n >= 1 && n <= 9) whack(n - 1, e);\n  });\n})();\n</script>\n</body>\n</html>\n")}
